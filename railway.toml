[build]
builder = "nixpacks"
buildCommand = "rm -rf node_modules/.cache && npm ci && npm run build && npm run db:deploy"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
